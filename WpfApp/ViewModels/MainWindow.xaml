<Window x:Class="WpfApp.ViewModels.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="ImportCSV" 
                Content="Import CSV" 
                HorizontalAlignment="Left"
                Grid.Row="0"
                Margin="10"
                VerticalAlignment="Center" 
                Height="30" Width="90" 
                Click="ImportCSV_Click" 
                RenderTransformOrigin="0.565,0.224"/>
        <DataGrid x:Name="DataGridDocuments"
          Grid.Row="2"
                  Margin="10"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          SelectionChanged="CSV_SelectionChanged"
          MouseDoubleClick="DataGridDocuments_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" />
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" />
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" />
                <DataGridTextColumn Header="City" Binding="{Binding City}" />
                <DataGridTextColumn Header="Items" Binding="{Binding ItemCount}" />
            </DataGrid.Columns>
        </DataGrid>

        <!--~<Button Content="Button" HorizontalAlignment="Right" VerticalAlignment="Top" Click="Button_Click"/>~-->
        <TextBox x:Name="SearchBox"
         Grid.Row="1"
         Width="250"
         Height="30"
         VerticalContentAlignment="Center"
         Margin="10"
         HorizontalAlignment="Left"
         TextChanged="SearchBox_TextChanged"/>

    </Grid>
</Window>
