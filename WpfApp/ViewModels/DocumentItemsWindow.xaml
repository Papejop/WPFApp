<Window x:Class="WpfApp.DocumentItemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="DocumentItemsWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="221*"/>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition Width="179*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="DocumentItemDataGrid" 
                  d:ItemsSource="{d:SampleData ItemCount=5}"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  AutoGenerateColumns="False"
                  CanUserDeleteRows="False" SelectionChanged="DocumentItemDataGrid_SelectionChanged" Grid.ColumnSpan="3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ordinal" Binding="{Binding Ordinal}" />
                <DataGridTextColumn Header="Product" Binding="{Binding Product}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
                <DataGridTextColumn Header="TaxRate" Binding="{Binding TaxRate}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
